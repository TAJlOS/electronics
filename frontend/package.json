{
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build --mode development && node postbuild.js",
    "build-heroku": "vue-cli-service build --mode production && node postbuild.js",
    "lint": "vue-cli-service lint"
  }
}
